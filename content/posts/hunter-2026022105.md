---
title: "Jsonh:JSON 终于能写注释了,还能省略逗号和引号"
date: 2026-02-21T05:23:00+08:00
draft: false
description: "Jsonh 是一个全新的 JSON 超集,支持注释,省略逗号,多行字符串,无引号键值等特性.兼容 JSON 和 JSON5,号称'HJSON v2',让配置文件真正为人类设计."
tags: ["JSON", "配置文件", "开源工具", "数据格式", "开发者工具"]
categories: ["技术工具", "开源"]
cover:
 image: "https://cfpic.890214.net/file/1771622680685_cover.jpg"
 alt: "Jsonh - JSON for Humans"
---

## 一句话总结
**Jsonh 让 JSON 终于像人类语言了**--写注释,省略逗号,多行字符串,无引号键值,还能兼容现有的 JSON 和 JSON5.

## 痛点:JSON 反人类的地方
每个写过配置文件的人都懂:

```json
{
 "name": "my-app",
 "version": "1.0.0",
 "description": "A very long description that needs to be on one line..."
}
```

- 不能写注释(解释为什么这样配置), 最后一个属性不能加逗号(git diff 很丑), 多行字符串必须用 `\n`(阅读噩梦), 键必须加引号(手累)

YAML 和 TOML 试图解决这些问题,但 YAML 的缩进地狱和 TOML 的嵌套语法又带来了新麻烦.

## Jsonh 的解法:JSON + 人性化语法
Jsonh 是 JSON 的超集,意味着**所有合法 JSON 都是合法 Jsonh**.但它增加了这些救命特性:

### 1. 三种注释方式
```jsonh
 // 行注释
 # 也是行注释
 /* 块注释 */
 name: "my-app"

### 2. 逗号可选
a: 1
 b: 2
 c: 3 // 不需要逗号!

### 3. 多行字符串
haiku: '''
 Let me die in spring
 beneath the cherry blossoms
 while the moon is full.
 '''

### 4. 无引号键值
keys: without quotes
 isn\'t: {
 that: cool?

### 5. 兼容 JSON5
支持十六进制数字,转义换行等 JSON5 特性:

 hex: 0xDEADCAFE
 fraction: .5e3
 bigNum: 1_000_000 // 下划线分隔

## 与现有格式对比
| 注释 | | | | | | |
| 尾随逗号 | | | | | | |
| 省略逗号 | | | | | | |
| 多行字符串 | | | | | | |
| 无引号键 | | | | | | |
| 十六进制 | | | | | | |
| 兼容 JSON | | | | | | |
| 兼容 JSON5 | | | | | | |

Jsonh 的定位很明确:**HJSON v2**--吸取 HJSON 的教训,同时保持与 JSON/JSON5 的兼容性.

## 实际应用场景

### 1. 项目配置文件
// 数据库配置
 host: localhost
 port: 5432

 // 生产环境覆盖
 production: {
 host: prod.db.internal
 ssl: true

### 2. API 文档示例
// 用户对象示例
 user: {
 id: 12345
 name: "张三"
 bio: '''
 全栈开发者,
 热爱开源和技术分享.

### 3. 游戏配置
player: {
 health: 100
 speed: 5.5
 inventory: [
 sword
 shield
 potion
 ]

## 快速开始

### 在线体验
直接访问:https://jsonh-org.github.io/Jsonh

### 安装解析器
**Node.js:**
```bash
npm install jsonh-ts

**Python:**
pip install JsonhPy

**Rust:**
cargo install jsonh_rs

**C#:**
dotnet add package JsonhCs

# 安装
dotnet tool install --global JsonhCli

# 转换 Jsonh 到 JSON
jsonh convert config.jsonh -o config.json

# 验证语法
jsonh validate config.jsonh

## 为什么不用 YAML/TOML?
**YAML 的问题:**
- 缩进敏感(少一个空格就报错)
- 多行字符串语法复杂(`>`, `|`, `>-`, `>+` 傻傻分不清)
- 甚至不是 JSON 的超集!

**TOML 的问题:**
- 嵌套对象语法反直觉(`[[table]]` 是什么鬼), 深层嵌套时配置分散在各处, 复杂对象还是得用 JSON 语法

**Jsonh 的优势:**
- 基于 JSON 语法,学习成本为零, 层级结构清晰(不像 TOML 那样分散), 严格的语法规则(不像 YAML 那样容易出错)

## 写在最后
Jsonh 不是什么革命性发明,它只是**把开发者需要的实用特性整合到一个格式里**.

如果你厌倦了 JSON 的严格,YAML 的诡异,TOML 的分散,Jsonh 可能是你配置文件的新选择.

**项目链接:** https://github.com/jsonh-org/Jsonh
**在线演示:** https://jsonh-org.github.io/Jsonh

## 时效性验证
- **HN 提交时间**: 20 分钟前(Show HN: Jsonh (JSON for Humans))
- **搜索引擎最早时间**: GitHub 仓库已存在,HN 首发 20 分钟前
- **结论**: 最早时间 ≤ 2 小时,符合新鲜热点标准